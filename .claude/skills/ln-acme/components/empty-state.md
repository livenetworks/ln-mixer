# ln-acme — Empty State Implementation

> For WHAT empty states must have → global ui/components/empty-state.md.

Use `<article class="section-empty">` with heading, message, and CTA button. No ln-acme-specific component — pure HTML + CSS.
