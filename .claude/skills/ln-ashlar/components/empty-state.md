# ln-ashlar — Empty State Implementation

> For WHAT empty states must have → global ui/components/empty-state.md.

Use `<article class="section-empty">` with heading, message, and CTA button. No ln-ashlar-specific component — pure HTML + CSS.
