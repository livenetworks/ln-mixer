{
	"name": "ln-mixer",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "npm run build:vendor && npm run build:css",
		"build:vendor": "vite build --config vite.vendor.config.js",
		"build:css": "sass assets/scss/app.scss assets/css/app.css --style=compressed --no-source-map",
		"preview": "vite preview"
	},
	"dependencies": {
		"wavesurfer.js": "^7.0.0"
	},
	"devDependencies": {
		"sass-embedded": "^1.83.0",
		"vite": "^6.0.0"
	}
}
